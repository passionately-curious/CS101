#include <simplecpp>

/*
main_program {
	int n, heads, tails;
	n = heads = tails = 0;
	cin >> n;

	for(int i = 0; i < n; i++)
		if(rand() % 2 == 0)
			heads++;
		else
			tails++;

	cout << "H: " << heads << ", T: " << tails << endl;
}
*/

#include 

main_pogram {
	int n, heads, tails;
	n = heads = tails = 0;
